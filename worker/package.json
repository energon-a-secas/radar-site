{
  "name": "radar-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for Radar — CSN tremors, Metro status, and official alert feeds",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
